services:
  web:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./db.sqlite3:/app/db.sqlite3   # сохраняем БД на хосте
      - ./media:/app/media             # сохраняем загруженные картинки
    environment:
      - DEBUG=1
